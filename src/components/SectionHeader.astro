---
type Props = {
  title: string;
  description?: string;
};

const { title, description } = Astro.props;
---

<div class="mb-5">
  <h2 class="text-2xl font-bold text-ink">{title}</h2>
  {description && <p class="mt-2 max-w-3xl text-sm leading-6 text-zinc-600">{description}</p>}
</div>
